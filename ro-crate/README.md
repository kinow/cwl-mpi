# CWL & MPI — RO-Crate

This directory contains the files used to generate an RO-Crate metadata
file for the files in this dataset.

The output is automatically validated with GitHub Actions, using the
[rocrate-validator](https://rocrate-validator.readthedocs.io/en/stable/).
